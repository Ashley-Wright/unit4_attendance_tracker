= render "layouts/student_nav"
%h2 Student
- if current_student
  %h2= current_student.name
  %h3 Courses
  - @student_enrollments.each do |enrollment|
    = link_to enrollment.course.title, course_path(enrollment.course)
  = link_to 'add a course', courses_path