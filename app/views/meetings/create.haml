= render "layouts/flash"
.row
  .small-12.columns
    %h2= Date.today.strftime('%a %d %b %Y')
    %h3= @code

:javascript
  setTimeout('delayedRedirect()', 30000);

  function delayedRedirect(){
    window.location.href = document.referrer;
  }