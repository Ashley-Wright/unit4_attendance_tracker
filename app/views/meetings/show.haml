= render "layouts/instructor_nav"
= render "layouts/flash"
.row
  .small-12.columns
    %h2= @meeting.created_at.strftime('%a %d %b %Y')
    %h4 Students
    %ul
      - @meeting_attendances.each do |attendance|
        %li= attendance.student.name